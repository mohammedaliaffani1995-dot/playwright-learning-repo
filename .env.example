BASE_URL=https://example.com
API_BASE_URL=https://jsonplaceholder.typicode.com
# JSONPlaceholder does not require a token.
# For a real API, get this token from your API provider, developer portal,
# login/auth endpoint, or QA team test credentials. Never commit a real token.
API_TOKEN=replace-with-demo-token-only
API_TIMEOUT_MS=3000
