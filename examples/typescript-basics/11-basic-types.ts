const username: string = 'qa.user';
const retryCount: number = 2;
const isActive: boolean = true;
const roles: string[] = ['tester', 'reviewer'];

console.log(username);
console.log(retryCount);
console.log(isActive);
console.log(roles);

export {};
